/* const expect = require('chai').expect
const parDeMeias = require('../src/parDeMeias.js')

describe('Quantos pares de meias Joana consegue vender?', () => {
  it('Quantidade de meias, () => {
      quantidade = n;
      expect(meias).to.be.a('function')
  })
  it('Meia cor black e branca', (preta, branca) => {
      if (corpreta = corbranca)
  })
})
 */